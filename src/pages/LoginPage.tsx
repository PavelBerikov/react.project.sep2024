import {FC} from 'react';
import LoginForm from "../components/LoginForm.tsx";

const LoginPage:FC = () => {
    return (
        <div>
            <LoginForm/>
        </div>
    );
};

export default LoginPage;